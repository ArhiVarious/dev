declare @area_id                 int;
declare @complex_id              int;
declare @complex2_id             int;
declare @medical_product_name_id int;
declare @medical_product_id      int;
declare @ds_medical_product_id   int;
declare @PageIndex               int;
declare @PageSize                int;
declare @stat_query_id           int;
declare @firm_id                 int;
declare @dd_id                   int;
declare @session_num             int;
declare @new_query               int;
declare @guid_str                varchar(036);
declare @url_referrer            varchar(250);
declare @search_str 	         varchar(250);
declare @brouser_str             varchar(250);
declare @path                    varchar(250);
declare @host                    varchar(250);
declare @stat_inet_ip            varchar(250);
declare @stat_inet_params        varchar(250);
declare @stat_inet_UrlReferrer   varchar(250);
declare @session_id              varchar(250);
